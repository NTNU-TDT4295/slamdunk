#!/usr/bin/expect

set ip [lindex $argv 0];
set pwd [lindex $argv 1];

spawn ssh xilinx@${ip} \
"echo $pwd | sudo -S bash /home/xilinx/setup_pynq.sh 2> /dev/null"
expect {
    password: {send "${pwd}\r"; exp_continue}
}
